import { FormEvent, useMemo, useState } from "react";
import {
  AlertTriangle,
  CheckCircle2,
  ChevronDown,
  CirclePlus,
  Clock3,
  FilePenLine,
  Filter,
  LifeBuoy,
  LockKeyhole,
  Plus,
  Ticket,
  X,
} from "lucide-react";
import { AppShell } from "@/components/medmap/AppShell";
import { Ticket as TicketRecord, ticketTypeOptions, useMedMap } from "@/lib/medmap-store";
import { cn } from "@/lib/utils";

const priorityStyles = { Low: "bg-slate-100 text-slate-500", Normal: "bg-[#eaf3ff] text-[#4a87c9]", High: "bg-[#fff2d9] text-[#9a6419]", Critical: "bg-[#ffe5e3] text-[#bd504d]" };
const statusStyles = { Open: "bg-slate-100 text-slate-500", Assigned: "bg-[#eaf3ff] text-[#4a87c9]", "In progress": "bg-[#f1edff] text-[#755bc0]", Pending: "bg-[#fff2d9] text-[#9a6419]", Completed: "bg-[#e8f8f2] text-[#168465]", Closed: "bg-[#e8f8f2] text-[#168465]" };

export default function Operations() {
  const { tickets, employees, addTicket, updateTicket } = useMedMap();
  const [showAdd, setShowAdd] = useState(false);
  const [filter, setFilter] = useState<"All" | TicketRecord["status"]>("All");
  const [draft, setDraft] = useState({ title: "", type: "General operations" as TicketRecord["type"], priority: "Normal" as TicketRecord["priority"], requesterId: employees[0]?.id ?? "", assigneeId: employees[1]?.id ?? "", dueDate: "2026-09-08", description: "" });
  const requester = employees.find((employee) => employee.id === draft.requesterId) ?? employees[0];
  const allowedTypes = requester?.ticketTypes ?? [];
  const eligibleAssignees = employees.filter((employee) => employee.ticketTypes.includes(draft.type));
  const filteredTickets = tickets.filter((ticket) => filter === "All" || ticket.status === filter);
  const openCount = tickets.filter((ticket) => !["Completed", "Closed"].includes(ticket.status)).length;
  const overdueCount = tickets.filter((ticket) => ticket.dueDate < "2026-09-05" && !["Completed", "Closed"].includes(ticket.status)).length;

  const updateDraft = (key: string, value: string) => setDraft((current) => ({ ...current, [key]: value }));
  const changeRequester = (id: string) => { const nextRequester = employees.find((employee) => employee.id === id); const nextType = nextRequester?.ticketTypes[0] ?? "General operations"; const nextAssignee = employees.find((employee) => employee.ticketTypes.includes(nextType)); setDraft((current) => ({ ...current, requesterId: id, type: nextType, assigneeId: nextAssignee?.id ?? current.assigneeId })); };
  const submit = (event: FormEvent) => { event.preventDefault(); if (!requester || !requester.ticketTypes.includes(draft.type) || !draft.title.trim() || !draft.description.trim()) return; addTicket({ title: draft.title, type: draft.type, priority: draft.priority, status: "Open", requester: requester.name, assigneeId: draft.assigneeId, createdAt: "2026-09-05", dueDate: draft.dueDate, description: draft.description }); setDraft((current) => ({ ...current, title: "", description: "" })); setShowAdd(false); };

  return <AppShell><div className="mx-auto max-w-[1440px] px-5 pb-12 pt-8 sm:px-8 xl:px-10"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 flex items-center gap-2 text-[11px] font-bold uppercase tracking-[0.17em] text-[#d4784f]"><LifeBuoy size={13} /> Operations & accountability</div><h1 className="font-display text-[32px] font-bold tracking-[-0.06em] text-[#152239] sm:text-[39px]">Make every piece of work accountable.</h1><p className="mt-2 max-w-[700px] text-[13px] leading-6 text-slate-500">Log company-wide tickets, respect each employee’s ticket permissions, assign an owner and keep the full work record editable.</p></div><button onClick={() => setShowAdd(true)} className="inline-flex items-center gap-2 self-start rounded-xl bg-[#182842] px-3.5 py-2.5 text-[11px] font-bold text-white shadow-[0_5px_12px_rgba(20,35,58,.15)] hover:bg-[#233958] md:self-auto"><Plus size={15} /> Log a ticket</button></div>

    <div className="mt-7 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200/80 bg-white p-4 shadow-[0_5px_20px_rgba(21,36,58,0.035)]"><div className="flex items-center justify-between"><p className="text-[10px] text-slate-400">Open work</p><Ticket size={16} className="text-[#d4784f]" /></div><p className="mt-2 font-display text-[26px] font-bold tracking-[-0.06em] text-[#152239]">{openCount}</p><p className="mt-1 text-[10px] font-semibold text-[#b47720]">{overdueCount} overdue</p></div><div className="rounded-2xl border border-slate-200/80 bg-white p-4 shadow-[0_5px_20px_rgba(21,36,58,0.035)]"><div className="flex items-center justify-between"><p className="text-[10px] text-slate-400">Assigned</p><CheckCircle2 size={16} className="text-[#1b9975]" /></div><p className="mt-2 font-display text-[26px] font-bold tracking-[-0.06em] text-[#152239]">{tickets.filter((ticket) => ticket.assigneeId).length}</p><p className="mt-1 text-[10px] font-semibold text-[#168465]">With a clear owner</p></div><div className="rounded-2xl border border-slate-200/80 bg-white p-4 shadow-[0_5px_20px_rgba(21,36,58,0.035)]"><div className="flex items-center justify-between"><p className="text-[10px] text-slate-400">Permission sets</p><LockKeyhole size={16} className="text-[#755bc0]" /></div><p className="mt-2 font-display text-[26px] font-bold tracking-[-0.06em] text-[#152239]">{new Set(employees.map((employee) => employee.ticketTypes.join("|"))).size}</p><p className="mt-1 text-[10px] font-semibold text-[#755bc0]">Employee-specific</p></div></div>

    {showAdd && <form onSubmit={submit} className="mt-5 rounded-2xl border border-[#bfe9da] bg-[#f2fcf8] p-5 sm:p-6"><div className="flex items-start justify-between"><div><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-lg bg-white text-[#168465]"><CirclePlus size={16} /></span><h2 className="font-display text-[16px] font-bold tracking-[-0.03em] text-[#152239]">Log company ticket</h2></div><p className="mt-2 text-[11px] text-slate-500">The logger can only choose ticket types granted on their employee record.</p></div><button type="button" onClick={() => setShowAdd(false)} className="rounded-lg p-1.5 text-slate-400 hover:bg-white" aria-label="Close ticket form"><X size={16} /></button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><label className="text-[10px] font-bold text-slate-500 sm:col-span-2">Title<input required value={draft.title} onChange={(e) => updateDraft("title", e.target.value)} className="form-input" placeholder="What needs to happen?" /></label><label className="text-[10px] font-bold text-slate-500">Logged by<select value={draft.requesterId} onChange={(e) => changeRequester(e.target.value)} className="form-input">{employees.map((employee) => <option key={employee.id} value={employee.id}>{employee.name}</option>)}</select></label><label className="text-[10px] font-bold text-slate-500">Ticket type<select value={draft.type} onChange={(e) => { const nextType = e.target.value as TicketRecord["type"]; const assignee = employees.find((employee) => employee.ticketTypes.includes(nextType)); setDraft((current) => ({ ...current, type: nextType, assigneeId: assignee?.id ?? current.assigneeId })); }} className="form-input">{allowedTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label className="text-[10px] font-bold text-slate-500">Priority<select value={draft.priority} onChange={(e) => updateDraft("priority", e.target.value)} className="form-input"><option>Low</option><option>Normal</option><option>High</option><option>Critical</option></select></label><label className="text-[10px] font-bold text-slate-500">Assign to<select value={draft.assigneeId} onChange={(e) => updateDraft("assigneeId", e.target.value)} className="form-input">{eligibleAssignees.map((employee) => <option key={employee.id} value={employee.id}>{employee.name}</option>)}</select></label><label className="text-[10px] font-bold text-slate-500">Due date<input required type="date" value={draft.dueDate} onChange={(e) => updateDraft("dueDate", e.target.value)} className="form-input" /></label><label className="text-[10px] font-bold text-slate-500 sm:col-span-2 lg:col-span-4">Description<textarea required value={draft.description} onChange={(e) => updateDraft("description", e.target.value)} rows={3} className="form-input resize-none" placeholder="Capture the expected result and any evidence needed." /></label></div><div className="mt-4 flex flex-wrap items-center justify-between gap-2"><p className="text-[10px] text-slate-400">Allowed logger types: {allowedTypes.join(" · ") || "None assigned"}</p><button type="submit" disabled={!requester || !requester.ticketTypes.includes(draft.type)} className="rounded-xl bg-[#182842] px-4 py-2.5 text-[11px] font-bold text-white disabled:cursor-not-allowed disabled:opacity-40">Create ticket</button></div></form>}

    <section className="mt-5 rounded-2xl border border-slate-200/80 bg-white shadow-[0_5px_20px_rgba(21,36,58,0.035)]"><div className="flex flex-col gap-4 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><div className="flex items-center gap-2"><span className="grid size-8 place-items-center rounded-lg bg-[#fff1e9] text-[#d4784f]"><Ticket size={16} /></span><h2 className="font-display text-[16px] font-bold tracking-[-0.03em] text-[#152239]">Central ticket engine</h2></div><p className="mt-2 text-[11px] text-slate-400">Open → Assigned → In progress → Pending → Completed → Closed</p></div><div className="flex flex-wrap gap-1 rounded-lg border border-slate-200 bg-slate-50 p-1"><button onClick={() => setFilter("All")} className={cn("rounded-md px-2.5 py-1.5 text-[10px] font-bold", filter === "All" ? "bg-white text-slate-700 shadow-sm" : "text-slate-400")}>All</button>{["Open", "Assigned", "In progress", "Pending", "Completed"].map((status) => <button key={status} onClick={() => setFilter(status as TicketRecord["status"])} className={cn("rounded-md px-2.5 py-1.5 text-[10px] font-bold", filter === status ? "bg-white text-[#168465] shadow-sm" : "text-slate-400")}>{status}</button>)}</div></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left"><thead><tr className="border-b border-slate-100 text-[10px] uppercase tracking-[0.12em] text-slate-400"><th className="px-5 py-3 font-bold sm:px-6">Ticket</th><th className="px-3 py-3 font-bold">Type</th><th className="px-3 py-3 font-bold">Owner</th><th className="px-3 py-3 font-bold">Priority</th><th className="px-3 py-3 font-bold">Due</th><th className="px-5 py-3 text-right font-bold sm:px-6">Status</th></tr></thead><tbody>{filteredTickets.map((ticket) => { const assignee = employees.find((employee) => employee.id === ticket.assigneeId); return <tr key={ticket.id} className="border-b border-slate-50 text-[11px] last:border-0 hover:bg-slate-50/70"><td className="px-5 py-3.5 sm:px-6"><p className="font-bold text-slate-700">{ticket.title}</p><p className="mt-1 text-[10px] text-slate-400">{ticket.id} · requested by {ticket.requester}</p></td><td className="px-3 py-3.5"><span className="rounded-full bg-slate-100 px-2 py-1 text-[9px] font-bold text-slate-500">{ticket.type}</span></td><td className="px-3 py-3.5 text-slate-600">{assignee?.name ?? "Unassigned"}</td><td className="px-3 py-3.5"><span className={cn("rounded-full px-2 py-1 text-[9px] font-bold", priorityStyles[ticket.priority])}>{ticket.priority}</span></td><td className={cn("px-3 py-3.5 text-slate-500", ticket.dueDate < "2026-09-05" && !["Completed", "Closed"].includes(ticket.status) && "font-bold text-[#bd504d]")}><span className="inline-flex items-center gap-1"><Clock3 size={12} />{ticket.dueDate}</span></td><td className="px-5 py-3.5 text-right sm:px-6"><select value={ticket.status} onChange={(e) => updateTicket(ticket.id, { status: e.target.value as TicketRecord["status"] })} className={cn("rounded-full border-0 px-2 py-1 text-[9px] font-bold outline-none", statusStyles[ticket.status])}>{["Open", "Assigned", "In progress", "Pending", "Completed", "Closed"].map((status) => <option key={status}>{status}</option>)}</select></td></tr>; })}</tbody></table></div>{filteredTickets.length === 0 && <div className="p-8 text-center text-[12px] text-slate-400">No tickets in this state.</div>}</section><div className="mt-5 flex items-center gap-2 rounded-2xl border border-[#f4d7ca] bg-[#fff8f4] p-4 text-[10px] leading-5 text-slate-500"><AlertTriangle size={15} className="shrink-0 text-[#d4784f]" /><span><span className="font-bold text-slate-700">Cases stay distinct:</span> use a customer Case for service interactions; use this central Ticket engine for work that needs an owner and outcome.</span></div></div></AppShell>;
}
